## Sistema de llamado para hospital

# Instalaciones necesarias

* PyQt5
```
sudo apt-get install python-pyqt5
sudo apt-get install python-pyqt5.qtsql
sudo apt-get install python-pyqt5.qtmultimedia
```
* Pydash
```
sudo pip install pydash
```